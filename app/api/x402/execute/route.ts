import { executeX402, X402Error, x402ErrorResponse } from "@/lib/server/x402";
import { policyAuditFingerprint, recordPolicyRejectionFromError } from "@/lib/server/activity-policy-audit";
export const runtime="nodejs"; export const maxDuration=180;
export async function POST(request:Request){let fingerprint=policyAuditFingerprint("invalid-request");try{const body=await request.json() as {id?:unknown;selectedIndex?:unknown};fingerprint=policyAuditFingerprint(body);if(typeof body.id!=="string"||typeof body.selectedIndex!=="number")throw new X402Error("Intent ID and option index are required.","INVALID_X402_SELECTION");return Response.json(await executeX402(body.id,body.selectedIndex));}catch(error){recordPolicyRejectionFromError(error,{source:"x402",operation:"execute",fingerprint});return x402ErrorResponse(error);}}
