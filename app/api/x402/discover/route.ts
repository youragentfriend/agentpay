import { prepareX402,X402Error,x402ErrorResponse } from "@/lib/server/x402";
export const runtime="nodejs";
export async function POST(request:Request){try{const body=await request.json() as Record<string,unknown>;if(typeof body.url!=="string")throw new X402Error("Resource URL is required.","INVALID_X402_URL");if(body.source!==undefined&&!['ai','catalog','direct'].includes(String(body.source)))throw new X402Error("Invalid discovery source.","INVALID_X402_SOURCE");return Response.json(await prepareX402({url:body.url,method:body.method,requestBody:body.requestBody,source:body.source as 'ai'|'catalog'|'direct'|undefined,catalogResourceId:typeof body.catalogResourceId==='string'?body.catalogResourceId:undefined,userRequest:typeof body.userRequest==='string'?body.userRequest:undefined}));}catch(error){return x402ErrorResponse(error);}}
