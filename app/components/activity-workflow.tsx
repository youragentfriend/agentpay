"use client";
import { useCallback, useEffect, useState } from "react";
type Event={id:string;source:string;activityType:string;status:string;statusGroup:string;statusCategory:string;amount?:string;asset?:string;title:string;summary:string;occurredAt:string;reference?:string};type Page={events:Event[];pagination:{page:number;limit:number;total:number;totalPages:number;hasNextPage:boolean;hasPreviousPage:boolean}};const empty:Page={events:[],pagination:{page:1,limit:25,total:0,totalPages:0,hasNextPage:false,hasPreviousPage:false}};
export function ActivityWorkflow(){const[data,setData]=useState<Page>(empty);const[status,setStatus]=useState("");const[source,setSource]=useState("");const[search,setSearch]=useState("");const[sort,setSort]=useState("newest");const[page,setPage]=useState(1);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");try{const q=new URLSearchParams({page:String(page),limit:"25",sort});if(status)q.set("statusGroup",status);if(source)q.set("source",source);if(search.trim())q.set("search",search.trim());const r=await fetch(`/api/payments/activity?${q}`,{cache:"no-store"});const x=await r.json();if(!r.ok)throw new Error(x.error||"Unable to load activity.");setData({events:x.events||[],pagination:x.pagination||empty.pagination});}catch(e){setError(e instanceof Error?e.message:"Unable to load activity.");}finally{setLoading(false)}},[page,search,sort,source,status]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),search?250:0);return()=>window.clearTimeout(timer)},[load,search]);function change(setter:(v:string)=>void,value:string){setter(value);setPage(1)}return <div className="activity-workspace"><div className="activity-toolbar"><label className="activity-search"><span>⌕</span><input value={search} onChange={e=>change(setSearch,e.target.value)} placeholder="Search activity"/></label><select value={status} onChange={e=>change(setStatus,e.target.value)}><option value="">All statuses</option><option value="awaiting-approval">Awaiting approval</option><option value="in-progress">Pending</option><option value="successful">Successful</option><option value="failed">Failed</option></select><select value={source} onChange={e=>change(setSource,e.target.value)}><option value="">All sources</option><option value="agentic-wallet">Agentic Wallet</option><option value="binance-pay">Binance Pay</option><option value="x402">x402</option></select><select value={sort} onChange={e=>change(setSort,e.target.value)}><option value="newest">Newest first</option><option value="oldest">Oldest first</option></select></div>{error&&<div className="workflow-error">{error}</div>}{loading?<div className="workflow-message">Loading activity…</div>:!data.events.length?<div className="empty-state"><div className="empty-icon">◷</div><h2>No matching activity</h2><p>Change or clear the filters to see other approvals, payments, and purchases.</p></div>:<div className="activity-table"><div className="activity-table-head"><span>Activity</span><span>Source</span><span>Status</span><span>Time</span></div>{data.events.map(event=><article className="activity-table-row" key={event.id}><div><strong>{event.title}</strong><small>{event.summary}{event.reference?` · ${event.reference}`:""}</small></div><span className="source-chip">{event.source.replace("agentic-wallet","Agentic Wallet").replace("binance-pay","Binance Pay")}</span><span className={`review-status ${event.statusCategory} ${event.statusGroup}`}>{event.statusGroup.replaceAll("-"," ")}</span><time>{new Date(event.occurredAt).toLocaleString()}</time></article>)}</div>}<div className="activity-pagination"><span>{data.pagination.total} activities · Page {data.pagination.page} of {Math.max(1,data.pagination.totalPages)}</span><div><button className="secondary-button" disabled={!data.pagination.hasPreviousPage||loading} onClick={()=>setPage(p=>p-1)}>Previous</button><button className="secondary-button" disabled={!data.pagination.hasNextPage||loading} onClick={()=>setPage(p=>p+1)}>Next</button></div></div></div>}
